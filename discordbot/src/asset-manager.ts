//! Manages the integration between this deno runtime and the node runtime of the
//! asset manager

//! This is done by assuming a checkout of this repo exists

export async function transaction() {}
